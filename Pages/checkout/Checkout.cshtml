@page
@model pizzaApp.Pages.checkout.checkoutModel
@{
}
 
@{
    var imagePath = "~/images/pizzas/"+(@Model.ImageTitle+".jpg");
}

<h1> Your Order !</h1>
<div class="align-content-center ">
    <div class="col-3 mx-auto mb-5">
        <img src="@imagePath" class="w-100" asp-append-version= "true" />"
        <p>Your order for the @Model.PizzaName pizza is ready</p>
        <p>Total price for your order is : @Model.PizzaPrice $</p>
        <br/>
        <a class="btn btn-primary mx-auto mx-1" asp-area="" asp-page="ThankYou">Confirm Order</a>
    </div>

</div>